fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pydantic==2.10.3
pydantic[email]==2.10.3
pydantic-settings==2.7.0
passlib[argon2]==1.7.4
pyjwt==2.13.0
python-multipart==0.0.32
slowapi==0.1.10

# testing
pytest==9.0.3
pytest-cov==6.0.0
httpx==0.28.1

# quality tooling (lint / type checking / dependency scanning) — CI-only,
# not needed at runtime, but kept in the same file as the rest of the
# project's dev/test deps rather than splitting into a second requirements
# file for three tools.
ruff==0.16.8
mypy==2.3.1
pip-audit==2.10.1
